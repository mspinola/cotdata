# Runtime dependencies for cotdata.
# Canonical source is pyproject.toml; prefer `uv pip install -e .` (see README).
# This file is a convenience mirror for pip-only / pinning workflows.
pandas>=1.5
pyarrow>=10
requests>=2.28
python-dateutil>=2.8
packaging

# Windows producer (Norgate) additionally needs the optional extra:
#   uv pip install -e ".[norgate]"     (or: pip install norgatedata)
# Dormant intraday/settlement-crosscheck provider:
#   uv pip install -e ".[databento]"

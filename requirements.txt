# Runtime dependencies for cotdata.
# Canonical source is pyproject.toml; prefer `uv pip install -e .` (see README).
# This file is a convenience mirror for pip-only / pinning workflows.
pandas>=1.5
pyarrow>=10
requests>=2.28
python-dateutil>=2.8
xlrd>=2.0

# Bars are NOT here: the Norgate and Yahoo producers moved to crucible-marketdata
# (ADR-0007), and so did their `norgate` / `yahoo` extras.
# The databento price provider stays (ADR-0006 alternative producer + the intraday
# work) and needs its optional extra:
#   uv pip install -e ".[databento]"
